# Release status

Current status: **Candidate** — tutorial source complete, awaiting clean-runtime execution. The pipeline package with its adaptation contract (`evaluate_zero_shot`, `adapt` with the frozen / unfrozen policies, `evaluate`, `predict_boxes`, `detect_adapted`, `save_artifact` / `from_artifact`; `detect` unchanged; the pinned Open Food Facts photo table and BYOD loaders in `samples.py`; AP@0.5 / AP@0.75 / mAP, the operating points and the fixed-box prior in `metrics.py`), offline unit tests (32, no weights required, plus 5 notebook-parity tests and 2 model-backed tests that run when the snapshot is staged), a local CPU execution of the committed notebook blob in the Windows venv (prior 2.7 % → zero-shot 9.7 % → new heads 37.6 % → selected policy `unfrozen last 2 decoder layers + new heads` 39.8 % AP@0.5 on 25 held-out photographs; recorded in `docs/release-verification.md`), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial notebook `tutorials/table_transformer_detection_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) with its static validator, generator parity checks and CI workflow exist. Unit tests and static conformance checks are expected to run in GitHub Actions. The earlier `TASK-INFERENCE` notebook's local fresh-kernel run is retained as history in `docs/release-verification.md` and is not evidence for the `E2E` blob. The tutorial remains non-release-grade until clean-runtime execution evidence for the exact candidate commit is recorded according to `docs/release-verification.md`.
